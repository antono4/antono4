interface:
  display_name: "Antono4"
  short_description: "Repo-specific patterns and workflows for antono4"
  default_prompt: "Use the antono4 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true